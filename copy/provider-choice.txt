Without a key, the graph, documents, search, memory, realms, views, handlers,
scheduled work and console still run because embedding happens on this machine.
Only chat and reasoning need a key, and you can add one any time.

To run chat and reasoning locally instead, see docs/guide/local-models.md. The
Models tab lists every job and where it runs.
