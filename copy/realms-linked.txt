Load a realm into a world with a path entry in config/realms.yml:

    path: /realms/<dir>

    embabel realms list        what the appliance can see
    embabel realms link ~/dev  keep your checkouts somewhere else — use the
                               directory containing your realms, not a realm
                               itself
