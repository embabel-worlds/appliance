Realms are read live: edit one and the change reaches the appliance
without a publish, push or restart.

Load one into a world with a path entry in its config/realms.yml:

    path: /realms/<dir>

    > embabel realms list        what the appliance can see
    > embabel realms link <dir>  point it somewhere else
