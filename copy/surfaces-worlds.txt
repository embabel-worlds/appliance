    {console}   {start_here}
                   The Worlds console: realms, documents, keys, views, chat.
                   Opens with the commissioning sequence.

    {api}   ((the server the console talks to))
    {dashboards}   ·   Metrics  {metrics}

    {mcp}         ((embabel-chat — chat clients, the assistant surface))
    {mcp_code}    ((embabel-code — coding agents: realms, authoring))
                   ((Authorization: Bearer — the token this setup just minted,))
                   ((stored at /data/embabel/assistant/admin/providers.env))
