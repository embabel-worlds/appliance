Documents you add are searchable once this appliance has an embedding model.
It has none yet: the local one is about a gigabyte and needs Docker Model
Runner turned on, so it is not pulled behind your back.

    embabel embeddings use local    on this machine, nothing leaves it
    embabel embeddings use hosted   the provider key you give the console

Either can be run later. Nothing already added is lost by waiting — turning a
model on indexes what is already there.
